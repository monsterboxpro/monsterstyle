.page_heading
  .page_title Button Groups

= section :variables do
  = row 'button_group_radius', '3px', 'button group radius'
= section :mixins do
  = row 'button_group', '$color_theme', 'button group styling'

= section :examples

.example
  = partial 'codes/html/button_group'
  .clear

= codes :html, :sass do
  = code :html, :button_group
  = code :sass, :button_group
